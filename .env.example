# Copy to .env and adjust as needed.
# Loaded by dotenvy in the app / picked up automatically by sqlx.
DATABASE_URL=postgres://pokertrainer:pokertrainer@localhost:5433/pokertrainer
SERVER_ADDR=127.0.0.1:8744
RUST_LOG=info
